<% # Clients of this partial must provide the following variable:
   # comment
%>

<% body = comment.is_deleted? ? t('commontator.comment.status.deleted_by', deleter_name: Commontator.commontator_name(comment.editor)) : comment.body %>
<%= simple_format body %>
